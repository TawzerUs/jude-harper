<nav class="bg-surface-inverse text-white">
  <div class="max-w-7xl mx-auto px-6 md:px-12 py-4 flex items-center justify-between">
    <a href="/admin" class="font-serif text-xl font-bold hover:text-gray-300 transition">Jude Harper — Admin</a>
    <div class="flex items-center gap-6 text-sm">
      <a href="/admin" class="<%= typeof page !== 'undefined' && page === 'dashboard' ? 'text-accent font-semibold' : 'text-gray-400 hover:text-white transition' %>">Dashboard</a>
      <a href="/admin/orders" class="<%= typeof page !== 'undefined' && page === 'orders' ? 'text-accent font-semibold' : 'text-gray-400 hover:text-white transition' %>">Orders</a>
      <a href="/admin/fulfillment" class="<%= typeof page !== 'undefined' && page === 'fulfillment' ? 'text-accent font-semibold' : 'text-gray-400 hover:text-white transition' %>">Fulfillment</a>
      <a href="/admin/bundles" class="<%= typeof page !== 'undefined' && page === 'bundles' ? 'text-accent font-semibold' : 'text-gray-400 hover:text-white transition' %>">Bundles</a>
      <a href="/admin/subscribers" class="<%= typeof page !== 'undefined' && page === 'subscribers' ? 'text-accent font-semibold' : 'text-gray-400 hover:text-white transition' %>">Subscribers</a>
      <a href="/" class="text-gray-400 hover:text-white transition">View Site &rarr;</a>
      <span class="text-gray-600">|</span>
      <span class="text-gray-500 text-xs"><%= typeof adminName !== 'undefined' && adminName ? adminName : 'Admin' %></span>
      <a href="/admin/logout" class="border border-gray-700 px-3 py-1 rounded-pill text-gray-400 hover:text-white hover:border-gray-500 transition text-xs">Logout</a>
    </div>
  </div>
</nav>
